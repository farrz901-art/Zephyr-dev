# P5-M4-S12 Source Contracts

This package makes retained source contracts explicit as technical, non-entitlement truth.

source contracts are technical, non-entitlement truth. They are not pricing tiers, subscription
tiers, license gates, or enterprise-grade claims.

Current bounded source truth:

- uns sources remain document-acquisition surfaces: one explicit document/blob/page-body
  selection enters the document partition path.
- it sources remain structured progress surfaces: bounded cursor/incremental reads emit
  records/items and preserve progress/checkpoint facts.
- All current retained source connectors remain Base technical-domain, non-enterprise connectors.
- `uns-stream` remains mixed: its non-extra runtime path is Base, while optional document extras are
  Pro-side capability truth outside default runtime marking.
- Extra remains reserved/incubating and is not default runtime source truth.

Machine-readable artifacts:

- `validation/p5_source_contract_matrix.json`
- `validation/p5_source_surface_classification.json`
- `validation/p5_source_usage_linkage.json`

Explicit non-claims:

- Source contracts are not billing, entitlement, or license semantics.
- The source models are not fully symmetric.
- Retained source truth is not a distributed source-runtime or enterprise connector claim.
