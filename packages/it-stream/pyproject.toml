[project]
name = "it-stream"
version = "0.1.0"
description = "Minimal structured-data flow package for Zephyr"
readme = "README.md"
authors = [
    { name = "farr-lesende", email = "1602058210@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "zephyr-core",
]

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
zephyr-core = { workspace = true }
