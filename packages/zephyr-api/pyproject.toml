[project]
name = "zephyr-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "farr-lesende", email = "1602058210@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
    "zephyr-core",
    "zephyr-ingest",
]

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
zephyr-core = { workspace = true }
zephyr-ingest = { workspace = true }
